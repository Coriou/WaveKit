# WaveKit Override Configuration for Local Production Testing
# Usage: docker-compose -f docker-compose.prod.yml -f docker-compose.override.yml up -d

services:
  wavekit-full:
    # Local development overrides
    build:
      context: .
      dockerfile: Dockerfile
      target: final
      cache_from:
        - type=local,src=./.docker-cache
      cache_to:
        - type=local,dest=./.docker-cache,mode=max
    image: wavekit:dev
    environment:
      WAVEKIT_LOG_LEVEL: debug
      NODE_ENV: development

  wavekit-sdrpp:
    build:
      context: .
      dockerfile: Dockerfile
      target: final-sdrpp
      cache_from:
        - type=local,src=./.docker-cache
      cache_to:
        - type=local,dest=./.docker-cache,mode=max
    image: wavekit:dev-sdrpp

  wavekit-core:
    build:
      context: .
      dockerfile: Dockerfile
      target: final-core
      cache_from:
        - type=local,src=./.docker-cache
      cache_to:
        - type=local,dest=./.docker-cache,mode=max
    image: wavekit:dev-core
